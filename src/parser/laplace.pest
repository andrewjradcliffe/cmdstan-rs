laplace = { "laplace" ~ (sep+ ~ laplace_term)* }

laplace_term = {
    mode
    | jacobian
    | draws
}

mode = ${
    !("mode" ~ (sep+ ~ "mode" ~ (&sep | EOI))+)
    ~ "mode" ~ ("=" ~ path)?
}
draws = ${
    !("draws" ~ (sep+ ~ "draws" ~ (&sep | EOI))+)
    ~ "draws" ~ ("=" ~ integer)?
}
